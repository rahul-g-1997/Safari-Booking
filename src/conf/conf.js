const config = {
  user_backend_url: String(import.meta.env.VITE_USER_BACKEND_URL),
  admin_backend_url: String(import.meta.env.VITE_ADMIN_BACKEND_URL),
  staff_backend_url: String(import.meta.env.VITE_STAFF_BACKEND_URL),
};
export default config;
